# PhysioCoach UX Session 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the open Session 2 recommendations from `physiocoach_ux_analysis.md`: progress visualizations, skeleton states, workout plan cards, toast feedback, and authenticated avatar affordances.

**Architecture:** Keep the work front-end first and compatible with the existing API. Use derived view models where the backend only exposes aggregates today, reuse the existing store/component patterns, and keep shared UI primitives small.

**Tech Stack:** Angular 21 standalone components, Tailwind 3 design tokens, PrimeNG 21, Vitest, optional Chart.js package.

---

### Task 1: Progress Visualizations

Agent: gpt-5.3-codex-spark / reasoning_effort=medium

**Files:**
- Modify: `package.json`
- Modify: `pnpm-lock.yaml`
- Modify: `src/app/features/progress/progress.page.ts`
- Modify: `src/app/features/progress/progress.page.html`
- Test: `src/app/features/progress/progress.page.spec.ts` if needed

- [ ] Add `chart.js` as an app dependency.
- [ ] Register Chart.js inside `ProgressPage` only in browser contexts.
- [ ] Render a weekly volume bar chart using available progress summary data.
- [ ] Render a GitHub-style streak heatmap from `summary.streakDays`.
- [ ] Render a body-weight sparkline from the latest measurement with a clear empty state until richer history exists.
- [ ] Replace emerald links or alerts in this page with brand/semantic token classes.
- [ ] Verify: `pnpm build`.

### Task 2: Skeleton Loading States

Agent: gpt-5.3-codex-spark / reasoning_effort=medium

**Files:**
- Create: `src/app/shared/ui/skeleton-block.component.ts`
- Modify: `src/app/features/progress/progress.page.html`
- Modify: `src/app/features/settings/settings.page.html`
- Modify: `src/app/features/measurements/measurements.page.html`
- Modify: `src/app/features/admin/admin.page.html`
- Modify: `src/app/features/posture-assessment/posture-assessment.page.html`
- Test: existing page tests, add focused tests only if a component contract is introduced

- [ ] Create a small standalone skeleton component that wraps the existing `.pc-skeleton` utility and accepts simple height/shape classes.
- [ ] Replace plain "Loading..." text blocks in async page states with skeleton blocks.
- [ ] Preserve accessible loading context with `aria-label` or visually readable section context.
- [ ] Keep workout plan generation's branded animation, but add skeleton blocks for the card body.
- [ ] Verify: `pnpm build`.

### Task 3: Workout Plan Exercise Card Redesign

Agent: gpt-5.3-codex-spark / reasoning_effort=high

**Files:**
- Modify: `src/app/features/workout-plan/workout-plan.page.ts`
- Modify: `src/app/features/workout-plan/workout-plan.page.html`
- Test: `src/app/features/workout-plan/workout-plan.page.spec.ts`

- [ ] Convert exercise rows to richer cards with muscle group tags, movement pattern tags, set/rep/rest metadata, and RPE rings.
- [ ] Preserve current expand/collapse behavior and full-width image expansion.
- [ ] Add image loading skeleton behavior without breaking lazy loading or fallback image tests.
- [ ] Improve keyboard handling so space does not scroll the page when toggling an exercise.
- [ ] Keep existing safety tips and notes in expanded details.
- [ ] Verify: `pnpm test src/app/features/workout-plan/workout-plan.page.spec.ts` and `pnpm build`.

### Task 4: Toast Feedback System

Agent: gpt-5.3-codex-spark / reasoning_effort=medium

**Files:**
- Modify: `src/app/app.config.ts`
- Modify: `src/app/core/layout/app-shell.component.ts`
- Modify: `src/app/core/layout/app-shell.component.html`
- Modify: `src/app/features/workout-plan/workout-plan.page.ts`
- Modify: `src/app/features/settings/settings.page.ts`

- [ ] Provide PrimeNG `MessageService` globally.
- [ ] Render a global `<p-toast>` once in the app shell.
- [ ] Show success/error toasts for workout feedback save, plan deletion failure/success, settings reload, and sign-out failure.
- [ ] Keep messages concise and non-blocking.
- [ ] Verify: `pnpm build`.

### Task 5: Authenticated Avatar in Header and Settings

Agent: gpt-5.3-codex-spark / reasoning_effort=medium

**Files:**
- Modify: `src/app/core/auth/auth.service.ts`
- Modify: `src/app/core/layout/app-shell.component.ts`
- Modify: `src/app/core/layout/app-shell.component.html`
- Modify: `src/app/features/settings/settings.page.ts`
- Modify: `src/app/features/settings/settings.page.html`

- [ ] Expose best-effort current authenticated user display name, initials, and image URL from the auth service.
- [ ] Replace desktop logout-only affordance with a compact avatar/profile cluster plus sign-out action.
- [ ] Add a profile summary block at the top of Settings with avatar, name, and authenticated status.
- [ ] Keep fallback initials deterministic when profile data is unavailable.
- [ ] Verify: `pnpm build`.

### Task 6: Final Verification and Polish

Agent: gpt-5.3-codex-spark / reasoning_effort=high

**Files:**
- Review all files changed by Tasks 1-5.

- [ ] Run `pnpm lint`.
- [ ] Run `pnpm build`.
- [ ] Run focused tests for changed pages.
- [ ] Note the pre-existing `localStorage.clear()` test-runner failure if full `pnpm test` still fails.
- [ ] Review mobile and desktop layout for text overflow and obvious visual regressions.
