import { AlertCircle, CheckCircle2, Info, X } from 'lucide-react'; import { clsx } from 'clsx';
export function Toast({ message, type = 'info', onClose }: { message: string; type?: 'success' | 'error' | 'info'; onClose?(): void }) { const Icon = type === 'success' ? CheckCircle2 : type === 'error' ? AlertCircle : Info; return <div role="alert" className={clsx('flex items-center gap-3 rounded-xl border p-4 text-sm font-semibold', type === 'success' && 'border-emerald-200 bg-emerald-50 text-emerald-800 dark:bg-emerald-950', type === 'error' && 'border-red-200 bg-red-50 text-red-800 dark:bg-red-950', type === 'info' && 'border-sky-200 bg-sky-50 text-sky-800 dark:bg-sky-950')}><Icon className="h-5 w-5 shrink-0" /><span className="flex-1">{message}</span>{onClose && <button onClick={onClose} aria-label="Dismiss"><X className="h-4 w-4" /></button>}</div>; }
